syntax = "proto3";

package redepth;

message PriceLevel {
    string price = 1;
    string amount = 2;
    uint64 order_count = 3;
}

message Depth {
    repeated PriceLevel asks = 1;
    repeated PriceLevel bids = 2;
    string updated_at = 3;
}